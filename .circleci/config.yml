# .circleci/config.yml
version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.11
    working_directory: ~/repo

    steps:
      - checkout  # Check out the code

      # Install dependencies (skip if you don't have requirements.txt)
      - run:
          name: Install dependencies
          command: echo "No dependencies to install"

      # Run tests (replace with your actual command)
      - run:
          name: Run tests
          command: echo "No tests configured yet"

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
